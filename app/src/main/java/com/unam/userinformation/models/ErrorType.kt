package com.unam.userinformation.models

enum class ErrorType {
    NAME_ERROR,
    LAST_NAME_ERROR,
    MOTHER_LAST_NAME_ERROR,
    ZIP_CODE_ERROR,
    SPORT_ERROR,
    DATE_OF_BIRTH_ERROR,
    OTHER
}